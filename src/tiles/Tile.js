import * as THREE from 'three';
class Tile{
    constructor(rotation) {
        this.rotation = rotation;
    }

    build_geometry(position) {
        console.log("Function not Implemented for class: Tile");
    }
}

export default Tile;